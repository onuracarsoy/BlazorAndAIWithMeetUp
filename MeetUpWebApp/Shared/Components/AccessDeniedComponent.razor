@page "/Account/AccessDenied"
@inject NavigationManager navigationManager

<div class="container my-4">
    <div class="card shadow-sm">
        <div class="card-header bg-danger text-white">
            <h3 class="mb-0">
                <i class="bi bi-lock-fill me-2"></i> Erişim Reddedildi
            </h3>
        </div>

        <div class="card-body">
            <p class="lead">
                Bu sayfaya erişme yetkiniz yok veya oturum açmanız gerekiyor.
            </p>

            <div class="row">
                <div class="col-sm-8">
                    <p class="text-muted">
                        Eğer hesabınızla giriş yaptıysanız ve hâlâ erişim göremiyorsanız,
                        lütfen yetkili bir kullanıcıyla iletişime geçin veya farklı bir hesapla tekrar deneyin.
                    </p>
                </div>
                <div class="col-sm-4 d-flex flex-column align-items-end gap-2">

                    <button class="btn btn-outline-secondary w-100" @onclick="GoHome">
                        <i class="bi bi-house-door me-1"></i> Anasayfaya Dön
                    </button>
                </div>
            </div>
        </div>
        <div class="card-footer text-muted small">
            Gerektiğinde yetki almak için yöneticinizle iletişime geçin.
        </div>
    </div>
</div>

@code {
    private void GoHome() => navigationManager.NavigateTo("/");
}
